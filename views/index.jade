extends layout

block content
  h1= title
  p Welcome to #{title}

    div.loginbox
    form(name="login", action="/schools/login", method="post")
      input(type="text", name="username")
      p
        input(type="password", name="password")
      input(type="submit", value="login")